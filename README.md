# flask-mongo-k8s
A Flask + MongoDB app deployed on Kubernetes using Deployments, StatefulSets, Services, PVCs, HPA, and Secrets. Includes autoscaling, authentication, resource limits, DNS-based service discovery, and full YAML manifests for Minikube deployment.
